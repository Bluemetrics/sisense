sudo: false

language: python

python:
  - "3.7"

install:
  - "pip install -U pip setuptools"
  - "pip install -r requirements.txt"

script:
  - "python -m unittest discover"

after_success:
  - "coveralls"